<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CALL_PHONE"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!-- Health Sensors -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Wear OS features -->
    <uses-feature android:name="android.hardware.type.watch" android:required="true" />
    <uses-feature android:name="android.hardware.screen.round" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.heartrate" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.step_counter" android:required="false" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/wear_network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CareBridgeWear">

        <!-- Standalone Wear App -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!-- Activities -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.google.android.wearable.intent.category.WEAR_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar" />

        <activity android:name=".CallActivity"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar" />

        <activity android:name=".MedicineActivity"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar" />

        <activity android:name=".HealthInfoActivity"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar" />

        <activity android:name=".GuardianActivity"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar" />

        <activity android:name=".HealthMonitorActivity"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar" />

        <activity android:name=".HeartRateDetailActivity"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar" />

        <activity android:name=".StepsDetailActivity"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar" />

        <activity android:name=".BloodOxygenDetailActivity"
            android:taskAffinity="${applicationId}"
            android:theme="@style/Theme.CareBridgeWear.NoActionBar" />



        <!-- FIXED: Firebase Messaging Service -->
        <service
            android:name=".service.WearFirebaseMessagingService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FIXED: Removed invalid permission -->
        <receiver
            android:name=".service.WearNotificationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="WEAR_MED_TAKEN" />
                <action android:name="WEAR_MED_NOT_TAKEN" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
