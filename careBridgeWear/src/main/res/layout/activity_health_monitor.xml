<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <FrameLayout
        android:id="@+id/health_monitor_screen"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black"
        android:padding="@dimen/padding_small">

        <!-- Header -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/padding_medium"
            android:paddingBottom="@dimen/padding_medium"
            android:background="@color/black">

            <ImageButton
                android:id="@+id/health_monitor_back_button"
                android:layout_width="@dimen/button_height_small"
                android:layout_height="@dimen/button_height_small"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:layout_marginStart="18dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_arrow_left"
                app:tint="@color/white" />

            <TextView
                android:id="@+id/health_monitor_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="@string/health_monitor_title"
                android:textColor="@color/white"
                android:layout_marginStart="15dp"
                android:textSize="@dimen/text_size_small"
                android:textStyle="bold" />

        </RelativeLayout>

        <!-- Simple WearableRecyclerView - shows all 3 items -->
        <androidx.wear.widget.WearableRecyclerView
            android:id="@+id/health_monitor_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="110dp"
            android:layout_marginTop="50dp"
            android:layout_marginBottom="50dp"
            android:clipToPadding="true"
            android:paddingTop="0dp"
            android:paddingBottom="0dp" />

    </FrameLayout>
</layout>