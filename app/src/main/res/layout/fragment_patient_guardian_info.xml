<?xml version="1.0" encoding="utf-8"?>
<androidx.swiperefreshlayout.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/swipeRefreshLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_patient_dashboard">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="@dimen/padding_medium">

        <!-- Shimmer Loading Placeholder: Shows loading animation while fetching guardian data -->
        <com.facebook.shimmer.ShimmerFrameLayout
            android:id="@+id/shimmerLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:shimmer_auto_start="true"
            app:shimmer_duration="1000"
            android:layout_marginBottom="@dimen/margin_medium">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- Placeholder Card 1: Loading state simulation for guardian card -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/placeholder_card_height"
                    android:layout_marginBottom="@dimen/margin_small"
                    android:background="@drawable/bg_rounded_white" />

                <!-- Placeholder Card 2: Loading state simulation for guardian card -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/placeholder_card_height"
                    android:layout_marginBottom="@dimen/margin_small"
                    android:background="@drawable/bg_rounded_white" />

                <!-- Placeholder Card 3: Loading state simulation for guardian card -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/placeholder_card_height"
                    android:background="@drawable/bg_rounded_white" />
            </LinearLayout>
        </com.facebook.shimmer.ShimmerFrameLayout>

        <!-- Warning Card: Displays connection or data loading errors to the user -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardWarning"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/margin_medium"
            android:visibility="gone"
            app:cardCornerRadius="@dimen/corner_radius_xlarge"
            app:strokeWidth="1dp"
            app:cardBackgroundColor="@color/white">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:padding="@dimen/padding_medium"
                android:gravity="center_vertical">

                <ImageView
                    android:layout_width="@dimen/icon_size_medium"
                    android:layout_height="@dimen/icon_size_medium"
                    android:src="@drawable/ic_warning"
                    android:layout_marginEnd="@dimen/margin_small"
                    android:contentDescription="@string/warning_icon_description"
                    app:tint="@color/warning_yellow" />

                <TextView
                    android:id="@+id/tvWarningMessage"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/guardian_load_error_message"
                    android:textColor="@color/text_primary"
                    android:textSize="@dimen/text_size_medium" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- No Guardian Message: Informs user when no guardian has been assigned -->
        <TextView
            android:id="@+id/tvNoGuardianMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/no_guardian_assigned_message"
            android:textColor="@color/text_secondary"
            android:textSize="@dimen/text_size_large"
            android:gravity="center"
            android:visibility="gone"
            android:padding="@dimen/padding_large"
            android:background="@drawable/bg_rounded_white"
            android:layout_marginBottom="@dimen/margin_medium" />

        <!-- Recycler View for Guardian Data: Displays list of assigned guardians -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewGuardians"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            android:layout_marginBottom="@dimen/margin_medium"/>
    </LinearLayout>
</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>