<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.carebridge">

    <!-- ✅ Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CareBridge">

        <!-- ✅ Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCt1z0X-4oq24pwiZOS6OPYzQ_ROmPvqmE" />

        <!-- ✅ Required for Google Play Services Maps -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- ✅ Splash Screen -->
        <activity
            android:name=".view.SplashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ✅ Login -->
        <activity
            android:name=".view.LoginActivity"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:exported="false" />

        <!-- ✅ Patient Dashboard -->
        <activity
            android:name=".view.PatientDashboardActivity"
            android:exported="false" />

        <!-- ✅ Guardian Dashboard -->
        <activity
            android:name=".view.GuardianDashboardActivity"
            android:exported="false" />

        <!-- ✅ Full Screen Map Activity -->
        <activity
            android:name=".view.FullMapActivity"
            android:exported="false" />
        <activity
            android:name=".view.FullMapActivityBlue"
            android:exported="false" />

    </application>

</manifest>
